[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps"
]

[phases.build]
cmds = [
  "npm run build",
  "ls -la dist/src/main.js || echo 'Build failed - dist/src/main.js not found'"
]

[start]
cmd = "npm run start:prod"

