{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd greengo-backend && npm install --legacy-peer-deps && npm run build"
  },
  "deploy": {
    "startCommand": "cd greengo-backend && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
